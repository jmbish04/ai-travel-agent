{
	"name": "travel-agent-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"db:migrate": "wrangler d1 migrations apply travel-agent-db",
		"db:list": "wrangler d1 migrations list travel-agent-db",
		"db:create": "wrangler d1 migrations create travel-agent-db",
		"db:execute": "wrangler d1 execute travel-agent-db",
		"db:status": "./migrate.sh status"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.43.0"
	},
	"dependencies": {
		"zod": "^4.1.12"
	}
}
